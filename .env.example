EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
EXPO_PUBLIC_RINGEE_API_URL=http://localhost:3000/api

# Telnyx WebRTC SIP credentials.
# In production these should be minted per-user via GET /webrtc/credentials
# (see lib/voice/TelnyxVoiceProvider.tsx). The env values below are used as a
# local dev fallback when the backend is unreachable or for shared sandbox
# testing.
EXPO_PUBLIC_TELNYX_SIP_USERNAME='useredison44068'
EXPO_PUBLIC_TELNYX_SIP_PASSWORD='G7bJ#!+Gtj=K'
# Optional human-readable label shown in Telnyx logs.
EXPO_PUBLIC_TELNYX_CALLER_NAME='Ringee Mobile'
